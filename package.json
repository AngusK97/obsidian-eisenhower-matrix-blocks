{
	"name": "obsidian-quadrant-tasks",
	"version": "2.0.1",
	"description": "Independent four-quadrant task boards embedded directly in Obsidian Markdown notes.",
	"private": true,
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/AngusK97/obsidian-quadrant-tasks.git"
	},
	"scripts": {
		"build": "esbuild src/main.js --bundle --external:obsidian --platform=node --format=cjs --target=es2018 --outfile=main.js",
		"test": "node --test tests/*.test.js",
		"check": "node --check src/core.js && node --check src/markdown-store.js && node --check src/board-store.js && node --check src/main.js",
		"verify": "npm run check && npm run build && npm test"
	},
	"devDependencies": {
		"esbuild": "0.25.6"
	}
}
